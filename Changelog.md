# 0.0.2 - Avoid confusion

After confusion between `//@protection` and `//@protected` in the [README.md](README.md), we decided to support both, and `//@protect` as well; fixes will still use `//@protection`

# 0.0.1 - Initial release

Allow for //@protection comments with an optional number of lines and a hash to throw error on code changes